# Environment
# 憑證一律只存在於 backend/.env（憲章後端約束、FR-085、SC-022）。
# 前端 MUST NOT 持有任何秘鑰；VITE_ 前綴的變數會被寫進建置產物，僅可承載公開資訊。
#
# （2026-08-04：移除舊有的「src/config.js 刻意不列入忽略」註記。該檔屬於舊架構，
#   其存在前提是瀏覽器直連 Supabase 並以 RLS 為防護，兩者皆已於憲章 v3.0.0 移除。
#   舊前端於過渡期保留為行為比對來源，將由 T183 一併刪除。）
.env
.env.*
!.env.example
# 正式環境的範本同樣只有欄位與說明、沒有任何值，MUST 進版控——
# 上面那條 `.env.*` 會連它一起忽略，因此需要這條例外。
# 少了它，部署到一半才發現「文件說有這個檔案，但 clone 下來沒有」。
!.env.production.example

# Python / 後端
__pycache__/
*.py[cod]
.venv/
backend/.venv/
.pytest_cache/
.ruff_cache/

# 上傳檔案（管理員房源照片與檢測圖；內容不進版控）
backend/uploads/*
!backend/uploads/.gitkeep

# Node / 前端
node_modules/
frontend/node_modules/
frontend/dist/
frontend/.vite/
# tsc -b 的增量建置快取。內容含本機絕對路徑，進版控只會製造無意義的衝突。
*.tsbuildinfo

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp
*.tmp

# Logs
*.log

# 稽核腳本的執行產物。**結論寫在 browser-acceptance.md**，這兩份是給當次除錯用的
# 原始資料：每跑一次就全部重寫，進版控只會是一長串讀不出意義的 diff。
specs/001-booking-site/checklists/responsive-audit.json
specs/001-booking-site/checklists/t181_flows.json
