# ⚠️ 前端不會讀取這個檔案。
#
# 本專案沒有建置步驟，瀏覽器讀不到 .env、process.env 或 import.meta.env
# （VITE_ 前綴的變數需要 Vite 打包，而本專案刻意不使用打包工具）。
#
# 執行期的憑證請填在 src/config.js，它會設定 window.__SUNNY_CONFIG__。
# 詳見 specs/001-booking-site/quickstart.md。
#
# 這個檔案只保留給日後可能的 CI 或命令列工具使用。
#
# 絕對不要在任何地方放入 service_role key —— 它可繞過所有 RLS 政策。
# 前端一律只使用 anon（publishable）key。

SUPABASE_URL=https://xxxxxxxxxxxxx.supabase.co
SUPABASE_ANON_KEY=your-anon-key
