# 自動部署的**備援**排程。每十五分鐘問一次 origin 有沒有新 commit。
#
# 平常的觸發來自 GitHub webhook（deploy/sunny-webhook.service），push 後幾秒
# 就會動。這個 timer 是接不到 webhook 時唯一會發現的東西——GitHub 那側投遞
# 失敗、接收器剛好在重啟、Caddy 正在換憑證，都會讓一次 push 靜靜地掉在地上。
#
# ⚠️ 兩者 MUST 同時存在。只留 webhook 的話，漏掉的那一次不會有任何跡象，
# 網站就這樣停在舊版直到下一次有人推東西。
#
# 沒有更新時 sunny-update.service 只做一次 `git fetch` 就結束（見腳本裡的
# early return，實測 195ms），所以這個間隔的成本幾乎是零。

[Unit]
Description=Sunny：備援輪詢，每十五分鐘檢查一次是否有新版本
Documentation=file:///opt/sunny/docs/deploy.md

[Timer]
Unit=sunny-update.service

# 開機後等兩分鐘再開始。Docker 與 Caddy 需要時間就位，一開機就衝進去建置
# 只會跟它們搶資源。
OnBootSec=2min

# ⚠️ 用 `OnUnitInactiveSec` 而不是 `OnUnitActiveSec`：前者從**上一輪結束**算起,
# 後者從上一輪開始算起。一次要跑十分鐘的建置若用後者，計時器在建置期間就會
# 反覆到期，結束當下立刻再排一輪。
OnUnitInactiveSec=15min

# 允許 systemd 把觸發時間對齊到附近，好跟其他 timer 併成同一次喚醒。
# 對一個備援機制，差一分鐘沒有意義。
AccuracySec=1min

# 刻意不設 Persistent=true。那是給「錯過的排程要補跑」用的，而這裡的工作是
# 冪等的輪詢——開機後兩分鐘自然就會問一次，不需要補一次關機期間錯過的。

[Install]
WantedBy=timers.target
