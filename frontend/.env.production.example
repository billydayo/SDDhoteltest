# 前端正式環境變數範本（Cloudflare Pages）。
#
# 用法：**不要**把值寫進檔案再上傳。到 Cloudflare Pages 的
# Settings → Environment variables → Production 逐一填入，Pages 會在建置時注入。
#
# ⚠️ VITE_ 前綴的變數會被原樣寫進建置產物，任何人打開瀏覽器都看得到。
# **只能承載公開資訊，MUST NOT 放任何秘鑰**（憲章前端約束、FR-085）。
# 資料庫密碼、JWT_SECRET、Google client secret 一律屬於後端，
# 只存在於 backend/.env。

# ---------------------------------------------------------------------------
# 後端 API 的基底位址
# ---------------------------------------------------------------------------
# 開發時這個變數是**不設定**的：src/api/client.ts 會退回相對路徑 `/api`，
# 由 Vite 的 proxy 轉給本機的 FastAPI。正式環境前後端不同源，必須指名。
#
# ⚠️ MUST 是 https://。前端頁面本身是 HTTPS，從 HTTPS 頁面對 http:// 發請求
# 會被瀏覽器當成 mixed content 直接擋掉——後端日誌上會一片空白，
# 因為那些請求根本沒送出去。
#
# ⚠️ 結尾不要加斜線，也不要帶路徑。client.ts 會自行接上端點路徑。
#
# 值填好之後，scripts/build-redirects.mjs 會用它產生 /uploads 的轉址規則
# （否則房源照片會全部變破圖）。兩者共用同一個變數，不會分歧。
VITE_API_BASE_URL=https://api.203-0-113-5.sslip.io

# ---------------------------------------------------------------------------
# 是否隱藏登入頁上的「管理員」示範帳密卡片
# ---------------------------------------------------------------------------
# 預設（不設定或設成 false）維持 FR-005 的行為：guest 與 admin 兩組帳密都印在
# 登入頁上。本機、內部展示、給人看作品時這是對的——審閱者不必註冊就能看後台。
#
# ⚠️ 但站台放上公網後，那張卡片等於把十二個後台模組的鑰匙印在首頁：
# 任何人都能改房價、讀客服訊息、看用戶清單。而本專案沒有修改密碼的端點，
# 「先上線再改密碼」這條路不存在。
#
# ⚠️ **要設就兩邊一起設。** 這個旗標 MUST 與後端 backend/.env 的
# SEED_ADMIN_PASSWORD 成對出現：
#
#   只隱藏卡片、不改密碼 → 密碼還是 README 裡的 admin123，藏起來的只是提示
#   只改密碼、不隱藏卡片 → 卡片印著一組已失效的密碼，點下去登入失敗
#
# 決定要公開展示後台，就兩個都別設；決定不公開，就兩個都設。
# VITE_HIDE_ADMIN_DEMO=true
