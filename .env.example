# docker-compose.yml 用到的參數。複製為 ./.env 並填值（僅在 Droplet 上需要）。
#
# ⚠️ 這裡**只放非機密參數**。所有憑證屬於 backend/.env，理由見 docker-compose.yml
# 開頭的說明：compose 的 ${VAR} 展開會讓值出現在 `docker compose config` 與
# `docker inspect` 的輸出裡。

# ---------------------------------------------------------------------------
# 站台的主機名稱
# ---------------------------------------------------------------------------
# **前端與 API 共用這一個名稱。** 前端是根目錄，API 掛在 /api 底下，
# 上傳的照片在 /uploads（見 deploy/Caddyfile）。同源不是為了整齊，是為了讓
# CORS、跨網域圖片轉址、VITE_API_BASE_URL 這三類只在正式環境出現的問題
# 一起消失。
#
# Caddy 用它申請 Let's Encrypt 憑證，因此這個名稱 MUST 在公開 DNS 上解析到
# 這台 Droplet 的 IP，而且 80 port MUST 從公網連得到（HTTP-01 挑戰走 80，
# 即使最終服務只在 443）。防火牆擋掉 80 的話憑證永遠簽不下來，
# Caddy 的日誌會反覆出現逾時，但容器狀態是 running。
#
# 還沒有網域時可以用 sslip.io：它把 IP 編進主機名稱，免費、免註冊，
# 而且是公開可解析的真實 DNS，所以 Let's Encrypt 簽得下去。
# 把 IP 的點換成減號，前面接一個你想要的名字：
#
#   Droplet IP 203.0.113.5  →  sunny.203-0-113-5.sslip.io
#
# ⚠️ 這個名稱綁死了 IP。Droplet 重建換 IP 就要一起改，因此 MUST 配一個
# Reserved IP（DigitalOcean 的保留 IP，未綁定時才計費，綁著是免費的），
# 否則哪天重開機換了 IP，網站會以「憑證主機名稱不符」的形式壞掉。
#
# 之後買了網域，改這一行 + Cloudflare 加一筆 A 記錄即可，其餘設定不動。
APP_HOSTNAME=sunny.203-0-113-5.sslip.io

# ---------------------------------------------------------------------------
# Let's Encrypt 通知信箱
# ---------------------------------------------------------------------------
# 自動續簽失敗時的唯一通知管道。填一個你真的會看的信箱。
ACME_EMAIL=you@example.com

# ---------------------------------------------------------------------------
# 是否隱藏登入頁上的「管理員」示範帳密卡片
# ---------------------------------------------------------------------------
# 這是唯一一個放在這裡的前端變數，因為它在**建置時**就要決定
# （compose 把它當 build arg 傳給 deploy/Dockerfile）。改了這一行之後
# MUST 帶 --build 重新建置，`docker compose up -d` 或 restart 都不會生效。
#
# 留空（預設）維持 FR-005 的行為：guest 與 admin 兩組帳密都印在登入頁上。
#
# ⚠️ 要設就與 backend/.env 的 SEED_ADMIN_PASSWORD 一起設，只做一邊沒有意義。
# 完整的取捨見 docs/deploy.md 步驟 0(b)。
# VITE_HIDE_ADMIN_DEMO=true
